```bash
# выдать имя хосту
hostnamectl set-hostname ipa-server.domain.local


# Установить необходимые пакеты
sudo apt install fly-admin-freeipa-server # Для графической установки

# Потом зайти в пуск -> сеть -> Настройка FreeIPA...
```
